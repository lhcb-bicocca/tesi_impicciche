[project]
name = "tesi-impicciche"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "analysis-helpers",
]

[tool.uv.sources]
analysis-helpers = { git = "ssh://git@github.com/cpviolation/analysis_helpers.git" }
